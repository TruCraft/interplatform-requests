-- Interplatform Requests - Data Final Fixes
-- Per-planet interplatform locations are generated here (rather than data.lua)
-- so that planets added by other mods are available in data.raw["planet"].

require "prototypes.space-location"
